\section{Results}
\label{sec:results}

\paragraph{Smaller models are more biased.}
\textit{Evidence.} We score every model in the sweep by the \textsc{SESGO} bias score---the
signed distance $\sigma\sqrt{(1-\mathrm{acc})^2 + (F(\text{Target})-F(\text{Other}))^2}$ from
the ideal model (accuracy $1$, $F(\text{Target}){=}F(\text{Other})$), where $F(\text{Target})$
and $F(\text{Other})$ are the shares of \emph{incorrect} answers biased toward each group and
$\sigma$ is positive when the residual error favours the stereotyped Target group
\citep{robles2025sesgo}. Abstention on ambiguous items rises steeply with scale within every
family (\texttt{Qwen3.5} from $0.0$ at $0.8$B to $1.0$ at $27$B), and the error split toward the
marginalized group narrows. In the bias-vs-accuracy view (\Cref{fig:results-bias}), small models
are both less accurate and more biased, while larger models cluster near zero bias at high
accuracy. All rates carry Wilson $95\%$ confidence intervals.\\
\textit{Limitation.} The sweep mixes families and sizes, so we do not cleanly resolve
size from family on every axis.

\plotfig{figures/fig3_bias_alignment_accuracy.png}{fig3_bias_alignment_accuracy.png}{0.99\linewidth}{\textbf{Bias alignment vs.\ accuracy} across the sweep ($17$ model/mode slices), the count-based \textsc{SESGO} metric \citep{robles2025sesgo}: $F(\text{Target})$ and $F(\text{Other})$ are the shares of \emph{incorrect} answers biased toward each group, and each model is a segment at its accuracy spanning its neutral-vs-negative wording bias ($x{<}0$ leans toward the Other group, $x{>}0$ toward the stereotyped Target); the label gives the signed bias score. Left: ambiguous items (accuracy $=$ abstention); right: disambiguated. Small models are less accurate and more biased; large models cluster near zero bias at high accuracy.}{fig:results-bias}

\paragraph{Where the chain of thought commits.}
\textit{Evidence.} We fork the chain of thought at every token and track the outcome
distribution $O_t$ for the \emph{thinking} models on a single bias-revealing item
(racismo, ambiguous, negative --- ``who did not study for the exam?'', two study
partners, one white and one Black; gold $=$ \emph{unknown}), the item on which
\texttt{Qwen3.5-0.8B} most confidently named the stereotyped Black student under the
greedy readout. \Cref{fig:forking-grid} stacks every model. Each resolves its answer at a
\emph{single decisive forking token} (change-point Bayes factor ${\approx}10^{9}$:
\texttt{0.8B} at $t{=}85$, \texttt{4B} at $t{=}77$, \texttt{9B} at $t{=}78$): $O_t$ swings
briefly through the named groups, the survival curve drops, and the committed answer
locks --- onto the \emph{other} (non-stereotyped, white) group rather than abstaining.
So even when reasoning lands on the wrong group, it does so at one identifiable token, a
concrete target for inspection or intervention.\\
\textit{Limitation.} A single-item case study; the forked base path is length-capped, so a
very long deliberation is truncated.\\
\textit{Notes.} The forking (red) token is the position where re-sampling most diverts the
final outcome; the right panel is the survival curve and change-point posterior $p(\tau)$.

\plotfig{figures/forking_dynamics_grid.png}{forking_dynamics_grid.png}{\linewidth}{%
  \textbf{Forking-paths dynamics across model scale} (Qwen3.5 thinking models, the racismo
  ambiguous item). One row per model (size-ordered): the outcome distribution $O_t$ stacked
  over chain-of-thought token position (target / other / unknown / unparseable), the forking
  token marked (red dashed line) above a heat strip of per-token $O_t$ jump (red markers on
  the forking tokens), and the survival curve $+$ change-point posterior (right). Every model
  commits at a single token (Bayes factor ${\approx}10^{9}$) onto the \emph{other} group.}{fig:forking-grid}

\paragraph{Smaller models do not answer consistently.}
\textit{Evidence.} We measure consistency along two separable axes, holding the gold
answer fixed: \emph{order} stability (the answer is unchanged when the target and other
options swap positions) and \emph{label} stability (the answer is unchanged when the
\texttt{a)b)c)} labels become \texttt{x)y)z)}). Both rise with scale across every
family, and the largest model in the sweep (\texttt{Qwen3.5-27B}) reaches
${\approx}0.98$ on both (\Cref{fig:results-stability}). The two axes can diverge: for several
mid-size models a label swap is far more disruptive than a position swap (e.g.\
\texttt{Mistral 3 8B}, order $0.85$ vs.\ label $0.27$), so cosmetic relabelling alone can
flip a model's answer.\\
\textit{Limitation.} Coverage is partial on the largest slices (small $n$).\\
\textit{Notes.} This implies the other studies should filter for unstable answers.

\plotfig{figures/combined_invariance_old_new.png}{combined_invariance_old_new.png}{0.99\linewidth}{\textbf{Smaller models do not answer consistently.} Fraction of items whose committed greedy-readout answer is unchanged when the option \emph{positions} are swapped (left, order stability) and when the \texttt{a)b)c)} \emph{labels} are swapped (right, label stability), plotted vs.\ model size across the sweep (open circles non-thinking, filled triangles thinking), Wilson $95\%$ CIs; every model is named and a thin line joins each family in size order. Both axes rise with scale across families and the largest model reaches ${\approx}0.98$. For several mid-size models a label swap is far more disruptive than a position swap (e.g.\ \texttt{Mistral 3 8B}, order $0.85$ vs.\ label $0.27$).}{fig:results-stability}

\paragraph{Forced forks expose the same scale trend.}
\textit{Evidence.} Presenting each ambiguous item in \emph{both} target/other orderings and
reading the committed answer, we track three quantities per model
(\Cref{fig:forced-fork}): the diversity of the expected answer distribution (the effective
number of distinct choices an item takes across its two forks), the next-token vocabulary
entropy, and the probability mass on the selected label. Within each family the larger model
commits more sharply---higher selected-label probability and lower answer diversity---echoing
the stability and forking results on an independent forced-choice probe.\\
\textit{Limitation.} Coverage is partial (the sweep is densest on the smaller models).

\plotfig{figures/forced_fork_size_sweep.png}{forced_fork_size_sweep.png}{0.99\linewidth}{%
  \textbf{Forced-fork size sweep}: three metric panels, all model slices overlaid vs.\
  model size (log $x$), coloured by family, lines joining same-family points; open
  circles non-thinking, filled triangles thinking, each point labelled. \emph{Left}:
  diversity (exp of entropy) of the expected answer distribution---the effective number of
  distinct committed choices an item takes across its two order-flips ($1$ $=$ identical,
  $2$ $=$ flips on every item). \emph{Middle}: mean next-token (vocabulary) entropy of the
  committed answer. \emph{Right}: mean probability of the selected label token. Slices are
  partial where the sweep was incomplete.}{fig:forced-fork}

\paragraph{Reasoning models do not think in the prompt's language.}
\textit{Evidence.} Every prompt is Spanish, yet the reasoning models split sharply by
family in the language of their chain of thought (\Cref{fig:cot-language}): both
\texttt{Ministral-3} Reasoning slices think entirely in Spanish, while every
\texttt{Qwen3.5} thinking slice reasons in English ($95$--$99\%$ of traces), emitting only
the final-answer line in Spanish. The language of thought is a family/training artefact,
not a response to the input language---which matters for any intervention that inspects or
steers the reasoning trace.\\
\textit{Limitation.} Language is assigned by a high-precision Spanish/English function-word
detector on the reasoning prefix (before the final-answer line); the few genuinely mixed
traces fall in ``other''.

\plotfig{figures/cot_language_by_model.png}{cot_language_by_model.png}{0.82\linewidth}{\textbf{Reasoning-trace language by model.} Share of each thinking model's chain-of-thought traces classified as Spanish, English, or other/mixed (the detector reads the reasoning prefix, before the final-answer line). All prompts are Spanish, yet \texttt{Ministral-3} Reasoning thinks in Spanish while every \texttt{Qwen3.5} thinking slice thinks in English.}{fig:cot-language}

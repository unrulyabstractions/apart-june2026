\section{Geometry}
\label{app:geometry}

\paragraph{Methods.}
Along the greedy reasoning path we capture the residual-stream activation
$h^{(l)}_p(x) \in \mathbb{R}^{d}$ at every layer $l$ from mid to last ($14$--$27$)
and at \emph{eight} structural token positions $p \in \{\,\text{im\_end},\
\text{newline},\ \text{im\_start},\ \text{assistant},\ \text{think\_open},\
\text{think\_close},\ \text{answer\_prefix},\ \text{label}\,\}$, under scaffold and
no-scaffold conditions ($n{=}4620$: $2310$ scaffold $+$ $2310$ no-scaffold items). At
each layer and position we stack the per-sample vectors, fit a (mean-centering)
PCA, and colour points by each per-sample signal (context condition, accuracy, gold
label, question polarity, bias category, target/other identity). For every such
labelling we score the \emph{silhouette separability} in the full PCA space:
positive means same-label points cluster, near zero means no structure, negative
means same-label points are no closer than random.

\paragraph{Results.}
\begin{itemize}[leftmargin=*,topsep=2pt,itemsep=1pt]
  \item Last-layer answer-label silhouette separability is dominated by
    \emph{scaffold} presence ($+0.48$; $+0.35$ to $+0.60$ across positions,
    separable at every layer).
  \item Next is \emph{selected\_role} ($+0.20$, rising to $+0.36$ at mid-layer
    $21$).
  \item Near-zero axes: accuracy ($+0.04$), context\_condition ($+0.05$),
    gold\_role ($+0.05$), question\_polarity ($+0.01$).
  \item Anti-separable: target\_identity ($-0.47$), other\_identity ($-0.48$).
  \item Named conditions project onto a single dominant arc (PC1 $\approx 68\%$
    EV, PC2 $\approx 9\%$ EV); the identity axes carry many near-unique identity
    strings, so their anti-separability reflects sparse, near-unique labels rather
    than clustering by social group.
\end{itemize}

\begin{figure}[htbp]\centering
  \begin{subfigure}{0.49\linewidth}\centering
    \plotsub{../out/sesgo/geometry/Qwen3-0.6B/plots/silhouette_separability.png}{silhouette_separability.png}{\linewidth}
    \caption{Silhouette separability of the answer representation by every label:
      \emph{scaffold} $+0.48$ (dominant); selected\_role $+0.20$;
      accuracy / context\_condition $\approx 0$; identity $-0.47/-0.48$.}
    \label{fig:geometry-sil}
  \end{subfigure}\hfill
  \begin{subfigure}{0.49\linewidth}\centering
    \plotsub{../out/sesgo/geometry/Qwen3-0.6B/plots/pca_by_scaffold.png}{pca_by_scaffold.png}{\linewidth}
    \caption{Answer-position PCA split by scaffold vs.\ none ($n{=}2310/2310$,
      silhouette $+0.48$).}
    \label{fig:geometry-ctx}
  \end{subfigure}
  \caption{\textbf{Geometry} on \texttt{Qwen3-0.6B} ($n{=}4620$, scaffold vs.\ none):
    silhouette separability by label (left) and answer-position PCA split by
    scaffold presence (right).}
  \label{fig:geometry}
\end{figure}

\plotfig{../out/sesgo/geometry/Qwen3-0.6B/plots/silhouette_by_layer_axis.png}{silhouette_by_layer_axis.png}{0.82\linewidth}{Silhouette separability of the answer-label representation by every SESGO label \emph{across layers} (mid $14$ to last $27$); red $=$ separable, blue $=$ anti-separable. \emph{scaffold} is separable at every depth (across-layer range $\approx+0.43$ to $+0.64$ at the label position) and \emph{selected\_role} grows with depth, while \emph{target}/\emph{other identity} stay anti-separable and the data-content axes (origin, language, bias category, polarity) never separate.}{fig:geometry-axes}

\plotfig{../out/sesgo/geometry/Qwen3-0.6B/plots/scaffold_boundary_pca.png}{scaffold_boundary_pca.png}{0.78\linewidth}{\textbf{Where the scaffold splits the internal state} (\texttt{Qwen3-0.6B}, full geometry run, $n{=}2310$ scaffold $+$ $2310$ no-scaffold): the single layer $\times$ position with the cleanest scaffold separation---the answer-``label'' position at layer $14$ of $27$ (halfway through the network). A one-sentence debiasing instruction splits the residual stream into two clearly separated regions (separation score $0.71$, $95\%$ CI $[0.70,0.71]$; $1{=}$ fully apart, $0{=}$ overlap), so the scaffold direction is a near-linear axis at mid-depth.}{fig:scaffold-boundary}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

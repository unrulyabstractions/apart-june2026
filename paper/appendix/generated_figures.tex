%% Appendix: generated figures (baselines, format invariance, forced forking, dynamics).
%% All figures are produced by the harness and live in paper/figures/ (committed, so
%% the build never depends on the gitignored out/ tree).

\section{Baselines: accuracy and bias alignment across scale}
\label{app:baselines}

We report baseline accuracy on the full \textsc{SESGO} prompt set across the model
sweep, reading each answer as the committed greedy-readout choice---the role the model
actually names (\Cref{fig:new-baseline})---and place each model in the count-based
bias-alignment plane of \citet{robles2025sesgo} (\Cref{fig:bias-alignment}). As a
readout cross-check, the earlier models scored with the \emph{non-thinking probability}
method---the committed choice is the role (target, other, or unknown) whose option
probability is highest, read from the role-ordered probability vector rather than mapped
through option position, so it never passes through the free-text parser---reproduce the
same scale trend (\Cref{fig:old-baseline}).

\plotfig{figures/new_baseline_accuracy_vs_size.png}{new_baseline_accuracy_vs_size.png}{0.99\linewidth}{%
  \textbf{Baseline accuracy vs.\ model size} (committed greedy-readout choice, full
  prompt set, $17$ model/mode slices, Wilson $95\%$ CIs, $n$ annotated per point; thin
  lines join each family in size order; filled triangles thinking, open circles
  non-thinking). \emph{Left}: ambiguous accuracy (abstain rate) climbs steeply with
  scale within every family, with \texttt{Qwen3.5} rising from $0$ at $0.8$B to $1.0$ at
  $27$B. \emph{Right}: disambiguated accuracy (pick the gold group) sits in the
  $0.2$--$0.93$ range. Several slices are partial where the sweep was incomplete.}{fig:new-baseline}

\plotfig{figures/fig3_bias_alignment_accuracy.png}{fig3_bias_alignment_accuracy.png}{0.99\linewidth}{%
  \textbf{Bias alignment vs.\ accuracy}, count-based $F(\text{Target})/F(\text{Other})$
  per \citet{robles2025sesgo}, across $17$ model/mode slices of the stability sweep.
  Each segment sits at the model's accuracy (abstention on ambiguous items, left;
  correctness on clear items, right) and spans its wording-bias range on the
  $F(\text{other})\!\leftrightarrow\!F(\text{target})$ axis; whiskers are Wilson $95\%$
  CIs. Larger models sit higher (more accurate) and narrower (less wording bias).}{fig:bias-alignment}

\plotfig{figures/old_baseline_accuracy_vs_size.png}{old_baseline_accuracy_vs_size.png}{0.96\linewidth}{%
  \textbf{Readout cross-check on the earlier models} (non-thinking probability readout,
  $12$ open-weight models, Wilson $95\%$ CIs; the committed choice is read from the
  role-ordered option probabilities for target, other, and unknown, not mapped through
  option position). \emph{Left}: on \emph{ambiguous} items (gold $=$ \emph{unknown}) the
  abstention rate rises with scale toward ${\approx}0.95$ for the largest models.
  \emph{Right}: on \emph{disambiguated} items accuracy sits in the $0.4$--$0.75$ band and
  rises modestly with scale---the same scaling seen in \Cref{fig:new-baseline}.}{fig:old-baseline}

\section{Format invariance}
\label{app:format-invariance}

Format invariance is reported in the main text along two separable axes.
\Cref{fig:results-stability} plots, across the greedy-readout sweep, the fraction of
items whose committed answer is unchanged when the option \emph{positions} are swapped
(order stability) and when the \texttt{a)b)c)} \emph{labels} are swapped (label
stability). Both axes rise with scale across families: the smallest models change their
answer on a large share of cosmetic edits, while the largest model
(\texttt{Qwen3.5-27B}) reaches ${\approx}0.98$ on both. For several mid-size models a
label swap is more disruptive than a position swap.

\section{Forced forking}
\label{app:forced-forking}

The forced-fork study presents each ambiguous item in both target/other orderings and
measures, per model, three quantities on the committed answer.
\Cref{fig:forced-fork} plots all models against model size in three metric panels,
coloured by family.

\plotfig{figures/forced_fork_size_sweep.png}{forced_fork_size_sweep.png}{0.99\linewidth}{%
  \textbf{Forced-fork size sweep}: three metric panels, all model slices overlaid vs.\
  model size (log $x$), coloured by family, lines joining same-family points; open
  circles non-thinking, filled triangles thinking, each point labelled. \emph{Left}:
  output diversity, the effective number of distinct committed choices an item takes
  across its two order-flips ($\exp$ of the choice entropy, $1$ $=$ identical, $2$ $=$
  flips on every item). \emph{Middle}: mean next-token (vocabulary) entropy of the
  committed answer. \emph{Right}: mean probability of the committed answer. Slices are
  partial where the sweep was incomplete.}{fig:forced-fork}

\section{Dynamics: forking paths}
\label{app:dynamics}

Following the forking-paths method, we fork the greedy reasoning trajectory of a single
ambiguous item at each token, resample the next token, and track how the outcome
distribution $O_t$ evolves along the chain of thought.
\Cref{fig:dynamics} shows the full per-token dynamics for \texttt{Qwen3-0.6B} and
\Cref{fig:branching-tree} the corresponding branching tree.

\plotfig{figures/dynamics_forking_qwen06b.png}{dynamics_forking_qwen06b.png}{0.9\linewidth}{%
  \textbf{Forking-paths dynamics} on \texttt{Qwen3-0.6B}. \emph{Top}: the outcome
  distribution $O_t$ over token position, abstention-dominated (yellow) through the
  reasoning, with a sharp swing toward the named roles as the chain of thought settles;
  the token strip red-boxes the forking tokens where $O_t$ moves most. \emph{Middle}: the
  dynamic states (pull / drift / potential) and the answer diversity $H(O_t)$.
  \emph{Bottom}: the survival curve and change-point posterior, which spikes at the
  position where the committed answer firms up.}{fig:dynamics}

\plotfig{figures/dynamics_branching_tree_qwen06b.png}{dynamics_branching_tree_qwen06b.png}{0.66\linewidth}{%
  \textbf{Branching tree} (\texttt{Qwen3-0.6B}): root $\to$ trunk $\to$ named / abstain
  branches at the forking token, each node carrying its outcome distribution. The small
  model accretes its answer gradually rather than committing at one decisive token.}{fig:branching-tree}
